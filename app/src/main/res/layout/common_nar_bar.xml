<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:background="@color/lighting_main_color"
                android:orientation="horizontal">

    <ImageView
        android:id="@+id/back"
        android:layout_width="35dp"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        android:src="@mipmap/nav_icon_back"/>

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_toRightOf="@id/back"
        android:gravity="center"
        android:textColor="@color/lighting_active_color"
        android:textSize="15sp"
        tools:text="新建"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:gravity="center">

        <TextView
            android:id="@+id/right_button"
            android:layout_width="52dp"
            android:layout_height="25dp"
            android:layout_marginRight="14dp"
            android:background="@drawable/lighting_nar_bar_save_shape"
            android:gravity="center"
            android:textColor="@color/lighting_main_color"
            android:textSize="14sp"
            tools:text="保存"/>
    </LinearLayout>

</RelativeLayout>